import React from 'react';

const About = () => {
  return <section style={styles.container}>About</section>;
};

const styles = {
  container: {
    height: '90rem',
    backgroundColor: 'black',
    color: 'white',
  },
};

export default About;
